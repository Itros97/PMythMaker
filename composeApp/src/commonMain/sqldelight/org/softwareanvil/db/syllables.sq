CREATE TABLE syllables (
    id INTEGER NOT NULL PRIMARY KEY AUTOINCREMENT,
    type TEXT NOT NULL,
    category TEXT NOT NULL,
    value TEXT NOT NULL,
    weight INTEGER NOT NULL DEFAULT 1
);

selectByType:
SELECT value, weight
FROM syllables
WHERE type = ? AND category = 'name';
